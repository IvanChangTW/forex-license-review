# 跨 AI Agent、跨機器長期記憶管理：演化進程 IV — 執行路徑 — 2026/08/31

> 承接[演化進程 III（2026-07-10、內容知識論）](memory-infra-evolution-2026-07-10.md)。
> 本篇整理 **2026-07-11 ～ 2026-08-31（52 天、934 個 commit）** 的演化。
> 一句話定位：**07/10 解決「內容會不會慢慢失真」；08/31 解決「規則會不會在動手的那一刻真的被執行」。**

- 快照數字為 2026-08-31 定格；之後變動不回寫本文，以 health 輸出為準。
- 本文是長背景／演化脈絡／對外說明書，**不是 source of truth**。
- **文中所有示意檔名皆為佔位符**，不對應真實檔案。

---

## 0. 52 天的數字

| 指標 | 值 |
|---|---|
| commits（07/11–08/31） | 934 |
| 自動化 gate | 12（前篇 3） |
| 教訓寫成記憶後，首次事後漏接的間隔 | 14／32／26／17／60 天，**中位數約 26 天** |
| 由機器／gate／流程抓到的漏接 | **0 次**（全部由人抓到） |
| 同步事故 | 三連發 → transaction 化 |
| 該事故真根因遲到 | **16 天** |
| 盲測命中 | 7／7（Non-Goal 2 trigger 未達成） |
| 被調高的 cap | **0** |

---

## 1. 主診斷：文字記憶的半衰期

方法：挑出「已建檔且明確寫了該怎麼做」的教訓條目，逐一回溯建檔**之後**的工作紀錄，找出同一個錯又犯一次的實例。

結果：至少 6 次事後漏接、分布在 4 條條目；首次漏接距建檔的中位數約 26 天；**由機器抓到的次數為 0**。

後續複查更難看：某條在同一場工作階段被引用 60 次，仍在當天被違反四次；三條核心條目累計違反 7、5、3 次。

> **文字記憶不在 shell 指令那一刻的執行路徑上。**

這不是說「寫記憶沒用」——那 6 次漏接的**診斷**之所以快，正因為條目寫得夠具體。而是說：記憶負責讓你**知道該做什麼**，不負責讓你**在該做的那一刻真的做**。前者是知識問題，後者是執行問題。

錯誤的修法（寫得更狠、加更多星號、重複更多次）已經走到頭：被引用 60 次仍當天違反四次，代表提高顯著性不再有邊際效益。

正確方向只有一個：**把檢查搬到動手的那一刻**。

---

## 2. 回應一：把檢查搬到執行路徑

gate 從 3 道長到 12 道，但**位置**比數量重要：

| 層 | 在哪裡跑 | 會不會被跳過 |
|---|---|---|
| ① 開場載入的文字規則 | 對話開始時進 context | **會**——只影響傾向，不影響控制流 |
| ② 健檢腳本 | 每日手動／半自動 | **會**——忘記跑等於不存在 |
| ③ 執行路徑上的檢查 | pre-commit、工具呼叫攔截、腳本內建斷言 | **不會**——它在動作與結果之間 |

代表性落點：

- **寫入前置的 fail-closed**：索引超過硬上限時中止。舊版是在**刪掉舊檔之後**才算，超限時舊索引已經沒了、且中止點卡在迴圈中間。修法是整段前移到任何寫入之前——超限就一個檔都不碰。門檻的環境變數**只准往下調**，設高於上限直接中止。
- **原子切換與自癒**：staging 目錄 → 逐檔雜湊比對 → 全符才做兩次 rename。錯誤立刻回滾；被硬殺則備份留著、下次自動復原。外面還有一把鎖。
- **條目層檢查器**：歷史層長期只有檔案層兩道 gate，**條目層零檢查**，缺口存在一個月。補上後新條目逐筆告警、舊條目只計數揭露。

**關於 enforcement 力道的一次來回**：7 月下旬把某道 gate 從警告升成硬擋，一週後降回——每次動作被卡的副作用比它防的問題更大。但這個決定在 8 月底被自己的證據推翻：正是那類「只警告不擋」的規則構成了上一節的漏接數字。⇒ **enforcement 強度不該由摩擦感決定，該由「違反的後果是否可逆」決定。**

---

## 3. 回應二：同步變成 transaction

| 時點 | 事件 |
|---|---|
| 第 1 次 | 某目錄記憶被同步成殘缺（少三分之一、索引檔不存在）＝在該目錄開新對話載到**零記憶**且無警示。只掛觀察條件「再犯就改原子 rename」 |
| 第 2 次（+5 天） | 同型再犯，中彈的是**當下這場對話自己的目錄**。條件達成 |
| 第 3 次（+2 天） | 第三次。隔天改成 transaction。症狀止血 |
| 第 16 天 | **真根因才釘死**：對話啟動 hook 有時間上限，超時會把同步砍在半路；而該 hook 的輸出被導進黑洞，**被砍本身完全不可見** |

值得記的不是修法，是這 16 天。transaction 化處理的是「被中斷後不要留爛攤子」，沒回答「為什麼被中斷」。**症狀消失讓根因的搜尋動機一起消失了。**

⇒ 判準應是「**我能不能說出它為什麼會發生**」，而不是「它還會不會再出現」。

---

## 4. 回應三：一支腳本的四種靜默失效

負責歷史層切檔封存的腳本很短，卻在 52 天內暴露四種**成因完全不同**的靜默失效：

| # | 失效 | 為什麼沒被發現 |
|---|---|---|
| 1 | **把最新的紀錄當成最舊的搬走**（假設「新的在上」，實際相反） | 搬走後那些內容本來就不該在原處——看起來完全正常。**已出貨至少兩次** |
| 2 | 在某平台一直寫錯換行字元 | 版控的自動轉換讓它在 diff 裡**完全不可見** |
| 3 | 在較舊直譯器版本直接崩潰，**壞了 15 天** | 平常不自動呼叫，只有人記得時才跑 |
| 4 | 切檔判斷邊界差一位 | 只在大小落在窄帶時顯現 |

第 1 項的修法示範了「修 bug」與「修可觀察性」的差別：除了改邏輯，加了一條**不變量斷言**——任何被封存的紀錄不得比留下的任何一筆更新，違反就拒寫。這條斷言刻意**不設開關、不只在測試跑**，因為它要擋的是「靜默把最新紀錄移出搜尋範圍」。

第 3 項推動了另一個改變：不再依賴人記得跑，改由同步流程超過門檻時自動呼叫，且**不論有沒有執行，狀態行都固定回報**。「沒有執行」從此是可觀察的事實。

> 寫下 SKIP 或 return 0 之前，先問：連續 N 次都走這條路，誰會知道？

---

## 5. 跨平台：兩端行為不同

| 實例 | 後果 |
|---|---|
| 某文字檢查的比對樣式在其中一端不受支援，且樣式開頭字元被當成命令選項 | 該檢查**在那一端從來沒真的跑過**——不是失敗，是沒執行 |
| 健檢告警訊息裡變數緊接全形標點 | 較舊的 shell 把全形字首位元組吃進變數名 → 嚴格模式中止 → **整支健檢崩掉**。一個月內三次 |
| 上條的反向測挖出更深的：該 shell 掛結束處理常式後，嚴格模式中止**一律回報成功** | 十次探針驗證。**同步腳本的錯誤退出碼有一整類失效**——也正是上一節那 16 天的推手 |

共同結構：**檢查本身沒壞，是「檢查有沒有跑」沒有被檢查。**

---

## 6. 方法外溢：另一個生產系統

同一套方法被搬到一個實際營運的門市管理系統（兩台主機、真實使用者）。8 月底新增一個「跨店唯讀檢視」權限旗標，兩個判斷值得記：

**看得到 vs 改得動，必須是兩套範圍。** 旗標語意是「唯讀」，但實作上很容易順手讓它也放寬寫入，那會讓一個報表勾選項悄悄變成改單權。做法是兩組範圍徹底分開、**明文禁止互相引用**：可見範圍吃旗標，可編輯範圍完全不看旗標、逐字鏡射後端既有守門。並用全矩陣測試綁死——前端比後端寬，使用者點下去才吃拒絕；比後端嚴，畫面就是在說謊。

**反向測才是真正的驗收。** 不是「跑測試全綠」，而是**把守門暫時拿掉、確認壞事真的會發生**：他店資料真的被繞過畫面的直接呼叫刪掉，裝回去就擋住。同一輪還坐實了一個既有的資料外洩——某查詢介面信任呼叫端傳來的範圍參數。它一直存在，是這次放寬把它從「難以利用」變成「可定向利用」。

**最後一哩：授權畫面本身也會過期。** 兩次放寬後，**授予這個權限的那一頁**說明文字仍停在第一版，明寫某類資料「仍鎖本店」——在第二次放寬後已是假的。機制全部正確、測試全部通過，但**人做授權決定時讀到的是舊描述**。而寫那道防護守則時又踩到一次假綠：第一版對整個檔案做字串比對，被同檔一段**完全無關**的文字滿足，反向測把目標字串拿掉照樣通過。**範圍沒鎖，等於沒測。**

---

## 7. 誠實邊界（本篇最重要）

### 一、《演化 III》宣稱的「0 斷鏈」，在寫下那天就不成立

前篇健康面寫「全部 0 斷鏈」。事實是當時的斷鏈檢查**只驗一種連結語法**，另一種從沒被機器驗過。8 月底補上後第一次跑就紅三條——其中兩條是**《演化 II》與《演化 III》自己在 repo 內的伴生檔**，指向前一篇時用中文標題而非檔名，從寫下那天就是斷的，各壞了約 57 天與 49 天。

這條錯誤的性質，正是這個系統 7 月底才建檔的一條教訓：**「沒找到」不等於「不存在」，負面結論必須附上檢查範圍。**

### 二、「語意漂移靠人工策展＋季度體檢」——分工線畫錯了

前篇把這條標成當時最重要的誠實邊界。第 1 節的數字推翻了它的**可行性**：若一條教訓平均 26 天就被第一次違反、且從不是機器發現，那「季度盤點」的間隔比失效週期長三倍。

需更正的不是「lint 抓不到語意漂移」（那句仍對），而是**結論**：不能因為機器抓不到，就交給頻率遠低於失效速度的人工流程。正確推論是把可機械化的部分往執行路徑搬，並接受剩下的部分**目前沒有解**。

### 三、本篇自己的兩個弱點

- 有一支檢查工具邏輯與測試齊備，但**沒有任何自動觸發點**——只在人記得時跑。
- 某道 gate 本次執行掃描零個目標卻回報通過；同 repo 另一支對同型問題有分母非空斷言，**這支沒有**。

兩條都不是「還沒做完」，是**已知會出事但還沒排進去**。

---

## 8. 07/10 vs 08/31 對照

| 面向 | 07/10（III） | 08/31（本篇） |
|---|---|---|
| 核心問題 | 內容會不會慢慢失真 | 規則會不會在動手那一刻被執行 |
| 自動化 gate | 3 | **12**，並分「在／不在執行路徑上」 |
| 同步 | 刪除後寫入 | staging + 雜湊 + 原子切換 + 自癒 + 鎖 |
| 守門時機 | 動作後檢查結果 | **動作前**；超限時一個檔都不碰 |
| 歷史層 | 檔案層兩道，條目層零 | 條目層檢查器上線 |
| 失效可觀察性 | 靜默（輸出進黑洞、退出碼失真） | 狀態行固定回報；「沒執行」也是事實 |
| 驗收標準 | 完工＝檢查點過 | **先讓它紅** |
| 知識來源 | 內查 + 外部生態對標 | **自己的失敗紀錄** |

---

## 9. 設計哲學：前篇 14 條全數存活，新增 3 條

52 天內沒有任何一條被推翻。兩條被強化的方式值得記：

- **Fail-closed by default**——超限時實測輸出就是「未觸碰任何目錄、全部保留舊版」，且門檻只准往下調。
- **點名綁定約束、追蹤它的遷移**——存活的方式是**它自己被違反後被證明是對的**。約束移動兩次：先是守門盯錯對象，後來發現真正的大宗根本不在被嚴格監控的那層（管最嚴的是最便宜的部分）。兩次都是被事後量測抓到，不是被規則預防。

新增：

| # | 原則 |
|---|---|
| 15 | **知識與執行是兩個問題。** 用前者的工具解後者的問題，會得到一條約 26 天就失效的曲線。 |
| 16 | **enforcement 強度由「後果是否可逆」決定，不由摩擦感決定。** |
| 17 | **檢查必須先證明自己有在檢查。** 任何「沒發現問題」都要附分母非空證據；守則上線前要先讓它紅一次。 |

---

## 10. Open threads（2026-08-31 時點）

- **語意漂移沒有機器解**。已知季度盤點接不住，但目前也沒有替代方案。這是最大的已知缺口。
- **有工具沒有觸發點**。
- **分母非空斷言尚未覆蓋所有 gate**。
- **另一端的編譯產物逼近警戒線**——約束又要遷移一次。
- **Non-Goal 2 仍成立**。盲測 7／7，引入向量檢索的觸發條件未達成。每次都要重新量測，不能因為「大家都在做」而放寬。

---

## 11. 結論

07/02 之後，系統知道自己**有沒有壞**；07/10 之後，知道自己**有沒有在慢慢變得不真**；08/31 之後，開始面對更難的問題——**它知道的事，在該用的時候到底在不在場。**

52 天、934 個 commit，最有價值的產出不是那 12 道 gate，是一組讓人不舒服的數字：教訓寫下來後約 26 天就被第一次違反，且沒有一次是機器發現的。這個數字沒有被藏起來當失敗，而是被當成設計輸入。

最能說明成熟度的同樣不是任何單項指標，是兩個瞬間：**新補的檢查第一次跑，就抓到前兩篇文章自己壞掉的連結**；以及**為了防止文案說謊而寫的守則，本身第一版就是假綠，被自己的反向測抓出來**。

> 原則未變：沒有資料庫、沒有 RAG、沒有常駐服務、沒有新記憶引擎——只有 Git ＋ Markdown ＋ health ＋ baton。改變的是它們現在站在動作與結果之間，而不是站在對話的開頭。

---

*成文：2026-08-31 家裡 Claude。資料來源：claude-memory 2026-07-11 至 2026-08-31 的 934 個 commit、各 gate 實際輸出、期間事故回溯紀錄。*
